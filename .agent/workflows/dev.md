---
description: Auto-run all git and dev commands for this project
---

// turbo-all

## PDF Voice Reader Development Workflow

1. Stage changes
```bash
git add .
```

2. Commit changes
```bash
git commit -m "Your message"
```

3. Push to GitHub
```bash
git push
```

4. Run local dev server
```bash
streamlit run app.py --server.headless true
```

5. Install dependencies
```bash
pip install -r requirements.txt
```
